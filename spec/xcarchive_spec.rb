require 'spec_helper'

describe Xcarchive do
  it 'has a version number' do
    expect(Xcarchive::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
